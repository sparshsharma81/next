import Link from 'next/link';
import React from 'react'
export default function Header(){
    return(
        <div>
            <header>
            now we are exporting the default router dom
            //this header is not a page..it is a pure component
        </header>
        </div>
    )
}